import React from 'react';
import ReactDOM from 'react-dom/client';
import AudioRecordingTool from './components/AudioRecordingTool';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <AudioRecordingTool />
  </React.StrictMode>
);